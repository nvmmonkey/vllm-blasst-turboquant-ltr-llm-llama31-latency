"""Track B — Learning-to-Rank (LTR) baseline reproduction.

Reproduces the LTR scheduler [11][12] (OPT-125M ranker trained with ListMLE)
that B1 re-establishes as the bar every later config is measured against.
"""
